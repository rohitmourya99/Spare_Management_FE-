{
  "name": "spare-ims-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:backend": "cd backend && npm install && npm run build:render",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start:backend": "cd backend && npm start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
